# 前端设计报告 01: 布局与架构 (Layout & Structure)

**日期**: 2026-01-02
**基于文档**: `PRODUCT_DESIGN_V2.5.md`

## 1. 设计哲学 (Design Philosophy)

核心理念：**Twitter 的即时性** x **Notion 的结构与深度**。

-   **视觉风格**: 极简主义 (Minimalist)。减少装饰性元素，强调内容 (Content-First)。
-   **UI 框架**: Tailwind CSS + Shadcn/ui。
-   **主题**: 支持 **深色 (Dark) / 浅色 (Light)** 模式切换，完美适配系统设置。默认偏向冷灰 ($slate) 色调以保持专业感。

---

## 2. 全局布局结构 (Global Layout)

采用经典的 **三栏式布局 (Three-Column Layout)**，这是最适合信息流与知识管理结合的形态。

### 2.1 桌面端布局 (Desktop, >1024px)

```
+----------------+--------------------------+-----------------------+
|  左侧导航栏     |       主内容区           |      右侧辅助栏        |
|  (Navigation)  |       (Timeline)         |      (Context)        |
|  w-64 (256px)  |       max-w-2xl          |      w-80 (320px)     |
|  Fixed         |       Scrollable         |      Hidden on iPad   |
+----------------+--------------------------+-----------------------+
```

#### A. 左侧导航栏 (Left Sidebar)
-   **顶部**: Logo (WhiteNote) + 当前用户头像/下拉菜单 (User Switcher)。
-   **主导航**:
    -   🏠 **Home** (时间线)
    -   🔍 **Explore** (搜索 & 发现)
    -   🔔 **Notifications** (提醒 & 消息)
    -   🔖 **Tags** (标签管理)
    -   🕸️ **Graph** (知识图谱)
    -   ⚙️ **Settings** (设置 & AI 配置)
-   **底部**: "Write" (新建笔记) 悬浮按钮 (Primary Button)。
-   **组件**: `NavigationMenu`, `Button`, `DropdownMenu` (from Shadcn).

#### B. 主内容区 (Main Content)
-   **顶部**: 
    -   **Header**: 页面标题 (如 "Home", "#React")，毛玻璃效果 (`backdrop-blur`).
    -   **Input Machine**: 类似 Twitter 的输入框，但集成 Tiptap 编辑器。支持 Slash Command (`/`).
-   **流 (Stream)**: 
    -   笔记卡片列表 (Infinite Scroll)。
    -   卡片设计：极简边框，重点突出文本。底部显示操作栏 (回复, 收藏, 查看图谱上下文)。

#### C. 右侧辅助栏 (Right Sidebar)
-   **搜索框**: 全局搜索入口 (Ctrl+K)。
-   **Mini Calendar**: 本月日历，高亮有笔记的日期。
-   **Trending Tags**: "热门标签" 或 "最近常访问的标签"。
-   **Backlinks Panel** (动态): 当在左侧点击某个具体笔记时，这里显示指向该笔记的反向链接。

---

## 3. UI 组件系统 (Component System)

我们不发明轮子，而是组合 **Shadcn/ui** 的原子组件。

### 3.1 核心组件映射

| UI 模块 | Shadcn 组件 | 定制化说明 |
| :--- | :--- | :--- |
| **容器/卡片** | `Card`, `CardHeader`, `CardContent` | 移除默认阴影，使用细边框 (`border-border`)，Hover 时轻微浮起。 |
| **输入框** | `Textarea` (基础), **Tiptap** (高级) | 通过 Headless UI 封装 Tiptap，外观模仿标准输入框。 |
| **按钮** | `Button` | Primary: 黑色(深色模式为白); Ghost: 用于图标按钮。 |
| **模态框** | `Dialog`, `Sheet` | Dialog 用于详情查看，Sheet 用于移动端菜单。 |
| **下拉菜单** | `DropdownMenu` | 用于每条笔记右上角的 "Edit/Delete/Pin" 操作。 |
| **头像** | `Avatar` | 圆形头像，支持 Fallback 文字。 |
| **Tab 切换** | `Tabs` |用于 "Private" vs "Public" 或 "Timeline" vs "Thread" 切换。 |
| **Toast** | `Sonner` (Shadcn 推荐) | 操作成功/失败的反馈。 |

### 3.2 图标系统
使用 **Lucide React**。风格统一，线条流畅。
-   Edit: `Pencil`
-   AI: `Sparkles` (重要标识)
-   Tag: `Hash`
-   Link: `Link2`

---

## 4. 主题配色 (Theming)

使用 `next-themes` 实现。

### Light Mode
-   **Background**: `bg-white` (#ffffff)
-   **Foreground**: `text-slate-900`
-   **Muted**: `text-slate-500`
-   **Border**: `border-slate-200`
-   **Primary**: `bg-slate-900` (黑底白字)

### Dark Mode
-   **Background**: `bg-slate-950` (#020617 - 接近纯黑但略带冷色)
-   **Foreground**: `text-slate-50`
-   **Muted**: `text-slate-400`
-   **Border**: `border-slate-800`
-   **Primary**: `bg-slate-50` (白底黑字)

---

## 5. 响应式策略 (Responsive Strategy)

Tailwind Breakpoints:
-   **sm (640px)**: 手机竖屏。隐藏 Sidebar。
-   **md (768px)**: 平板竖屏/大手机。
-   **lg (1024px)**: 平板横屏/小笔记本。显示左侧 Sidebar，隐藏右侧 Context。
-   **xl (1280px)**: 桌面标准。三栏全开。

## 6. CSS 技术栈

-   **Tailwind CSS**: 原子化样式 utility-first。
-   **Tailwind Typography (`prose`)**: 用于渲染 Markdown 内容 (Tiptap 输出)。
-   **Tailwind Animate**: 使用 `animate-in`, `fade-in`, `slide-in` 做微交互。
